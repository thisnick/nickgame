mode main()
: nmi game_nmi
    palette = example_palette
    ppu_upload_palette()
    {PPUCTRL}(PPUCTRL_NMI_ON)
    while true
        nmi

nmi game_nmi()
    ppu_upload_oam_poll_pads(0)
    {PPUMASK}(PPUMASK_BG_ON | PPUMASK_SPR_ON)
    ppu_reset_scroll(0, 0)
